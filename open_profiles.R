#############################################################################
# This script defines the open_profiles() function which returns a parameter, 
# its depth axis, its QC, and its date.
#############################################################################

require(ncdf4)
require(oce)
require(MASS)
require(stringr)
require(stringi)

open_profiles <- Function(profile, PARAM_NAME) {
	# profile is a full path to the file, PARAM_NAME is consistent
	# with bgc-argo denomination

	# TODO : implement

	return(0)
}

